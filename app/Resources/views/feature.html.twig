<div class="col-md-3 col-sm-6 feature">
    <div class="thumbnail">
        <img class="img-feature" src="{{ asset(image) }}" alt="">
        <div class="caption">
            <h3>{{ title }}</h3>
            <p>{{ info }}</p>
            <p>
                <a href="{{ path(path) }}" class="btn btn-primary">{{ caption|default("Przejdź") }}</a>
            </p>
        </div>
    </div>
</div>