<td>
    <div class="btn-toolbar btn-group-xs" role="group" aria-label="...">
        <a href="{{ path('client_show', { 'id': cell.value.id }) }}" class="btn btn-primary">szczegóły</a>
        <a href="{{ path('client_edit', { 'id': cell.value.id }) }}" class="btn btn-primary">edytuj</a>
    </div>
</td>